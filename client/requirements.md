## Packages
framer-motion | Page transitions and list animations
papaparse | Client-side CSV export functionality
@types/papaparse | Types for CSV export

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
